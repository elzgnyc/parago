// Copy this file to `src/config.js`, then set your Supabase project ref.
// `src/config.js` is gitignored so your ref stays out of version control.
// (The ref is not a secret — it appears in your Supabase URL — but keeping it
// out of the repo means forks don't accidentally point at your project.)
export const CONFIG = {
  functionsBaseUrl: 'https://<PROJECT_REF>.functions.supabase.co',
};
